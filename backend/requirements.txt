# OpenAI SDK — single path for all OpenAI-compatible providers
openai>=1.55.0

# Anthropic SDK — for models using Anthropic Messages API (MiniMax)
# anthropic>=0.49.0

# API Server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.0
python-multipart>=0.0.12

# HTTP Client
httpx>=0.28.0

# MCP Protocol (Model Context Protocol)
mcp>=1.26.0

# Template Engine (prompt builder)
jinja2>=3.1.0

# Validation
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Utilities
python-dotenv>=1.0.0

# YAML frontmatter parsing
pyyaml>=6.0

# Cron expression parsing
croniter>=6.0.0

# Database
asyncpg>=0.30.0
# sqlparse  (TODO: switch to alembic for proper migration history — see backend/.pending-bugs.md)

# OpenTelemetry (optional - install only if using OTLP tracing)
# opentelemetry-api>=1.30.0
# opentelemetry-sdk>=1.30.0
# opentelemetry-exporter-otlp-proto-grpc>=1.30.0
# opentelemetry-exporter-otlp-proto-http>=1.30.0

# Agent Communication Protocol

# Browser automation (Rust CLI via pip, used by computer_use tool)
agent-browser-cli>=0.20.0

# File-to-Markdown conversion (PDF, Office, images, audio → LLM-ready Markdown)
markitdown>=0.1.6

# Deep web crawling for AI agents (async, anti-bot, structured extraction)
crawl4ai>=0.8.7

# Image processing (visual diff via pixelmatch — same engine Playwright uses)
Pillow>=11.0.0
pixelmatch>=0.2.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0

# PTY bridge for interactive terminal streaming (POSIX-only)
ptyprocess>=0.7.0

# Blocking IO detection for async safety tests
blockbuster>=1.5.0

